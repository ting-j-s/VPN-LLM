server:
  tun_name: tun0
  tun_ip: 10.8.0.1
  tun_peer: 10.8.0.2
  mtu: 1400
  listen_mode: ssh

forwarding:
  enable_nat: false
  enable_route: true

transport:
  type: ssh

session:
  heartbeat_timeout: 30